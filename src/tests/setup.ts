import { expect } from 'vitest';
import '@testing-library/jest-dom';
// Attach jest-dom matchers to vitest's expect
// (vitest auto exposes expect, but explicit import ensures availability before extension)
